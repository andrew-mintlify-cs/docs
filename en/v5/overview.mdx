---
title: "SDK autogeneration overview"
description: "How this version demonstrates Mintlify SDK reference autogeneration from TypeDoc, DocFX, Javadoc, Sphinx, and phpDocumentor artifacts."
---

This **v5.0** version is a demo of Mintlify SDK reference autogeneration. Instead of hand-writing a page for every class and function, you point Mintlify at a doc-tool artifact and it builds the reference for you.

## How autogeneration works

Use the `sdk` navigation property on a tab in `docs.json`. Mintlify parses the artifact and creates navigation groups and pages for the library — one page per class, interface, module, and function, with cross-page links and search indexing included.

```json
"navigation": {
  "tabs": [
    {
      "tab": "SDK Reference",
      "sdk": {
        "format": "typedoc",
        "source": "sdk-artifacts/typedoc.json",
        "directory": "sdk/typescript"
      }
    }
  ]
}
```

<Note>
  You must declare `sdk` on a tab. A tab with `sdk` may include `groups`, but no other navigation structures, such as `pages`, `versions`, or `languages`. It also cannot include an `openapi`, `asyncapi`, or `graphql` property.
</Note>

## Configuration fields

<ParamField path="format" type="string" required>
  The documentation tool that produced the artifact: `typedoc`, `docfx`, `javadoc`, `sphinx`, or `phpdoc`.
</ParamField>

<ParamField path="source" type="string" required>
  Relative path to the artifact file or directory in your docs repository, or an HTTPS URL. Does not accept HTTP URLs.
</ParamField>

<ParamField path="directory" type="string">
  The URL path prefix for generated pages. Defaults to `sdk-reference`.
</ParamField>

Add multiple tabs to document multiple libraries. Use a unique `directory` for each library to avoid route collisions.

<Tip>
  Add your artifact directory to `.mintignore` so Mintlify treats artifacts as build inputs rather than publishing them as static assets.
</Tip>

## What you'll see in this demo

During the demo we add SDK tabs one format at a time (TypeScript, Java, .NET, Python, PHP). Each tab points at a real artifact under `sdk-artifacts/` (or a remote HTTPS jar for Java). After deploy, the generated reference pages appear under that tab automatically.

This Overview tab stays as the landing page for **v5.0** so you can explain the flow before the generated tabs are connected.
